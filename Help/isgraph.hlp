  
  Name:  isgraph
 
  #include <ctype.h>
  int isgraph(int ch);

  The isgraph( ) function returns nonzero if ch is any 
  printable character other than a space else zero is returned. 
  
  #include <ctype.h>
  #include <stdio.h>
  int main(void)
  { char ch;
    for(;;) {
    ch = getchar();
    if(isgraph(ch)) printf(''%c is printable\n", ch);
    if(ch == '.') break;}
    return 0;
  }
  
  Related Functions:  isalnum, isalpha, iscntrl, isdigit, isprint

