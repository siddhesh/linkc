
  Name:  tanh
  
  #include <math.h>
  float tanhf(float arg);
  
  The tanh family of functions return the hyperbolic tangent of arg.

  Example:
  #include <math.h>
  #include <stdio.h>
  int main(void)
  {
    double val = -1.0;
    do{ printf(''Hyperbolic tangent of %f is %f.\n",val,tanh(val));
         val += 0.1;
      } while(val<=1.0);
   return 0;
 }
 
  Related Functions: acos, asin, atan, atan2, cos

