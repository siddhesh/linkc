
  Name:  srand
  
  #include <stdlib.h>
  void srand(unsigned int seed);
  
  The srand( ) function sets a starting point for the sequence 
  generated by rand( ). 
    
  Example:
  #include <stdio.h>
  #include <stdlib.h>
  #include <time.h>
  int main(void)
  { int i, stime;long ltime;
    ltime = time(NULL);
    stime = (unsigned) ltime/2;srand(stime);
    for(i=0; i<10; i++) printf("%d ", rand());
    return 0;}
  
  Related Function:  rand

