
  Name:  seekdir
  
  #include <dirent.h>
  void seekdir(DIR *dirp, off_t offset);

  The seekdir() function sets the location in the directory stream 
  from which the next readdir call will start.  seekdir() should
  be used with an offset returned by telldir.The seekdir() function
  returns no value.
  
  Related Functions:  lseek, closedir, opendir, readdir, rewinddir

