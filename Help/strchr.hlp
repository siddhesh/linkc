  
  Name:  strchr
  
  #include <string.h>
  char *strchr(const char *str, int ch);

  The strchr( ) function returns a pointer to the first 
  occurrence of the low-order byte of ch in the string 
  pointed to by str. If no match is found,null is returned.

  #include <stdio.h>
  #include <string.h>
  int main(void)
  {
   char *p;
   p = strchr("this is a test", ' ');
   printf(p);
   return 0;
  }
 
  Related Functions : strpbrk,strspn,strstr,and strtok
