
  Name:  vscanf
  
  #include <stdio.h>
  #include <stdarg.h>
  int vscanf(const char *format, va_list ap);

  The functions vscanf are functionally equivalent to scanf( ), 
  fscanf( ), and sscanf( ),except that the argument list has been
  replaced by a pointer to a list of arguments. 

  Related Functions:  vprintf, vfprintf, vsprintf

