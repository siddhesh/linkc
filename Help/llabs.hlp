
  Name:  llabs
 
  #include <stdlib.h>
  long long int llabs(long long int num);
  
  The llabs( ) function returns the absolute value of num.
  It is similar to labs( ) except that it operates on values 
  of type long long int.llabs( ) was added by C99.

  Related Functions:  abs and labs

