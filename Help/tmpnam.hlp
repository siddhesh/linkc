
  Name:  tmpnam
    
  #include <stdio.h>
  char *tmpnam(char *name);
  
  The tmpnam( ) function generates a unique filename and
  stores it in the array pointed to by name.
  
  Example :
  #include <stdio.h>
  int main(void)
  {char name[40];
   int i;
   for(i=0; i<3; i++) {
   tmpnam(name);
   printf("%s ", name); 
   return 0;
  }

  Related Function:  tmpfile


