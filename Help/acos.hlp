
  Name:  acos
  
  #include <math.h>
  float acosf(float arg);
  double acos(double arg);
  long double acosl(long double arg);
  
  The acos( ) family of functions returns the arc cosine of arg. 
  
  Example:
  #include <math.h>
  #include <stdio.h>
  int main(void)
  { double val = -1.0;do{ 
      printf("Arc cosine of %f is %f.\n", val, acos(val));
      val += 0.1;
    } while(val<=1.0);
   return 0;} 
 
  Related Functions:  asin, atan, atan2, sin, cos, tan, sinh, cosh
